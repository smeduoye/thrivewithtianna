# Legacy standalone Caddy example (host-installed).
# Production uses the Caddy container in docker-compose.prod.yml — see Caddyfile.

app.thrivewithtianna.com {
	reverse_proxy thrivewithtianna-web:80
}
